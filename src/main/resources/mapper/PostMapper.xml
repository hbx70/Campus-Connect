<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hbx.campusconnect.mapper.PostMapper">
    <!-- sql -->
    <select id="getAllPost" resultType="com.hbx.campusconnect.pojo.PostList">
        select p.*, u.username, u.user_avatar, u.role, u.user_status from posts p
        left join users u on p.user_id = u.id
        <where>
            <if test="userId!=null">
                AND user_id=#{userId}
            </if>
            <if test="type != null">
                AND type = #{type}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <select id="getAllOwnPost" resultType="com.hbx.campusconnect.pojo.PostList">
        select p.*, u.username, u.user_avatar, u.role, u.user_status from posts p
        left join users u on p.user_id = u.id
        <where>
            p.user_id = #{userId}
            <if test="type!=null">
                AND p.type=#{type}
            </if>
            <if test="status != null">
                AND p.status = #{status}
            </if>
            <if test="title != null and title != ''">
                AND p.title LIKE CONCAT('%', #{title}, '%')
            </if>
        </where>
        ORDER BY p.created_at
        <choose>
            <when test="order == 'asc' or order == 'ASC'">
                ASC
            </when>
            <otherwise>
                DESC
            </otherwise>
        </choose>
    </select>

</mapper>